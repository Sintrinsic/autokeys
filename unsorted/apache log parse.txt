tail -10000 /usr/local/apache/logs/error_log|perl -lne 'if (/ModSecurity: Access denied/) { $modsec++ } elsif (/Request exceeded the limit of 10 internal redirects/) { $redirects++ } elsif (/RewriteCond: NoCase option for non-regex/) { $nocase++ } elsif (/cannot use a full URL in a 401 /) { $full401++ } else { print } END { print "Skipped modsec:$modsec redir:$redirects [NC]:$nocase 401:$full401" }'