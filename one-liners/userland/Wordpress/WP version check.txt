wpVersionCheck(){ grep -oP "DB_(NAME|USER|PASSWORD)[', ]*\S*" wp-config.php | sort -r | awk -F"['\"]" '{printf $3" "}END{printf "\n"}' | while read user pass db; do echo "File: $(grep -oP "(?<=wp_db_version = )[0-9]+" wp-includes/version.php)" ; echo "Database: $(mysql -NBe 'select option_value from wp_options where option_name = "db_version"' $db)" ; done| column -t; }; wpVersionCheck 