gswp(){ grep VmSwap /proc/*/status 2>/dev/null | sort -nk2;}; echo "$(gswp;sleep 10; gswp)" | awk -F'[/ \t]+' '{if($3 in procs){print $3,($5-procs[$3])}else{procs[$3]=$5}}' | sort -nk2 | tail -n 20 | while read pid size; do usr=$(stat --format="%U" /proc/$pid/status 2>/dev/null); pbin=$(grep -oP "(?<=Name\S).*" /proc/$pid/status 2>/dev/null); [[ $usr && $pbin ]] && echo "$pid $size $usr $pbin"; done | column -t 