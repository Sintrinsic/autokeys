lynx --dump --width=10000 localhost/whm-server-status | awk '$11~/^[0-9\.]/{doms[$12]+=$7}END{for(dom in doms){print doms[dom], dom}}' 2>/dev/null |  while read line; do echo $line" "$(grep -A5 " $(echo $line | cut -d" " -f2)" /usr/local/apache/conf/httpd.conf | grep -Po '(?<=DocumentRoot ).*'|cut -d/ -f3 ); done | awk '{usrs[$3]+=$1}END{for(usr in usrs){print usrs[usr]/1000,usr}}'| sed 's/\.[0-9]*//' | sort -n | tail -n5
