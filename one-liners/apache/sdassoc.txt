 sdassoc(){  hdom=$(grep -oP "$(echo avigreen.mozstoreweb.com | sed 's/\./\\\./g')[a-z0-9-]*" /usr/local/apache/conf/httpd.conf | uniq); domUser=$(grep "$hdom" /etc/userdomains | awk '{print $NF}'); grep $hdom /var/cpanel/userdata/$domUser/main | grep : | sed 's/://' | while read dom sub; do [[ $dom = "main_domain" ]] && echo $sub || echo "$dom: $sub" | cut -d: -f$2 ;done ;}; sdassoc