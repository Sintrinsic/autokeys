sdassoc(){ t=1; [[ $2 ]] && t=$2;  hdom=$(grep -oP "$(echo $1 | sed 's/\./\\\./g')[a-z0-9-]*" /usr/local/apache/conf/httpd.conf | grep -v 'bytes$' | uniq); domUser=$(grep "^$hdom" /etc/userdomains | awk '{print $NF}' | uniq | head -n1); grep -P "$hdom" /var/cpanel/userdata/$domUser/main | awk -F'[:-]+' '{if($1~/^ +$/||$1~/main_domain/){$1=$2};print $1, $2}' | awk -vnum=$t -vdom=$hdom '$1==dom||$2==dom{print $num}' ;}; sdassoc $(grep -B10 "$(pwd)$" /usr/local/apache/conf/httpd.conf | grep -Po '(?<=ServerName ).*' | uniq) 
