cbips(){ grep -oP "(?<=Assigning account )\w+" accounts.moved | while read line; do cip=$(grep -oP '(?<=IP=)[0-9\.]+' /var/cpanel/users/$line 2>/dev/null); [[ $cip == $1 || $cip == "" ]] || echo "$line $cip"; done;}; genout(){ cbips $1 | while read usr ip; do tip=$(awk '/'$usr'/{print $NF}' accounts.moved); echo "$usr $ip $tip $(grep $tip ips.added &>/dev/null && echo 'YES' || echo 'NO') "; done | sed '1i USER CURRENT TRANSFERRED ADDED' | column -t; printf "\nIPs needing removal:\n"; listips | grep -P "($(cat ips.added | perl -pe 'chomp if eof;s/\n/|/' ))";}; genout 